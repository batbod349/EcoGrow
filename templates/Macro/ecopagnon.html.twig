{% macro displayEcopagnonImage(moodValue, width=100, height=100) %}
    <img alt="Ecopagnon" src="{{ asset('images/ecopagnon/' ~ moodValue ~ '.png') }}" width="{{ width }}" height="{{ height }}">
{% endmacro %}

