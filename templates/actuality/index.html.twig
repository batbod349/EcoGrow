{% extends 'base.html.twig' %}

{% block title %}Actualités
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="flex flex-col items-center example-wrapper space-y-10">
		{% include 'components/headers/pageHeader.html.twig' with {
                'title': 'Actualites',
				'image_path': asset('/images/icons/Actu.svg')
            } %}

		<h3 class="font-inter text-xl font-bold">Derniers articles :</h3>

		<ul>
			<div class="flex flex-wrap justify-center items-center gap-5">
				{% for article in articles %}
					{% include 'components/cards/actualite.html.twig' with {
                'image': article.getImage,
                'image_alt': article.name,
                'title': article.name,
                'subtitle': article.text,
                'description': article.text,
            	} %}
				{% else %}
					Aucun article n'a été trouvé.
				{% endfor %}
			</div>
		</ul>
	</div>
{% endblock %}

{# <strong>{{ article.name }}</strong>
					                -
					                {{ article.createDate|date('Y-m-d H:i') }}
					                {% if article.getImage %}
					                    <img src="data:image/jpeg;base64,{{ article.getImage }}" alt="{{ article.name }}" width="250">
					                {% endif %}
					                {{ article.text|slice(0, 100) }}
					                {% if article.text|length > 100 %}
					                    ...
					                {% endif %}
					                #}
