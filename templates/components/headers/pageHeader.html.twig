<div class="d-flex align-items-center">
    <!-- Profil -->
    <div class="profile">
        <img src="{{ asset('path/to/profile-image.png') }}" alt="Profil" class="rounded-circle" width="40" height="40">
    </div>

    <!-- Classement -->
    <div class="ms-3 badge bg-dark text-white d-flex align-items-center">
        <span>1er</span>
        <span class="ms-2">
            <i class="bi bi-trophy-fill"></i> <!-- Icône pour le trophée -->
        </span>
    </div>

    <!-- Points -->
    <div class="ms-3 badge bg-success text-white d-flex align-items-center">
        <span>340</span>
        <span class="ms-2">
            <i class="bi bi-tv-fill"></i> <!-- Icône pour l'écran -->
        </span>
    </div>

    <!-- Objectif -->
    <div class="ms-3 badge bg-light text-success d-flex align-items-center">
        <span>2/3</span>
        <span class="ms-2">
            <i class="bi bi-check-circle-fill"></i> <!-- Icône pour l'objectif atteint -->
        </span>
    </div>
</div>