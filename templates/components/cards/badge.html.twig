{% set title = title|default('') %}
{% set src = src|default('') %}

<div class="flex flex-col items-center">
	<div>
		<img class="w-28 object-cover" src="{{ src }}"/>
	</div>
	<p class="font-semibold text-inter">{{ title }}</p>
</div>
