{% set title = title|default('') %}
{% set imagePath = imagePath|default('') %}

<div class="border-2 border-greenDark w-56 h-64 px-3 py-3 rounded-2xl space-y-1 justify-items-center bg-white">
	<div class="">
			<img src="{{ imagePath ? ('data:image/jpeg;base64,' ~ imagePath) : asset('images/default-profil/pepe-lunettes-doree.jpg') }}" alt="Image du tips" class=" h-44 object-cover">
	</div>
	<h3 class="text-center">{{title}}</h3>
</div>
